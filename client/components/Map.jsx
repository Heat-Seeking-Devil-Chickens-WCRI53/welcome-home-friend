import React from 'react';
import { GoogleMap, LoadScript, Marker, InfoWindow } from '@react-google-maps/api';

const mapStyle = {
    height: '100vh',
    width: '100%',
};

const center = {
    lat: 34.052,
    lng: -118.244
};